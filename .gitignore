# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# Media terupload runtime (backup terpisah, jangan ikut git).
# SELURUH storage/ berisi data PRIBADI warga (berkas permohonan KTP/KK di
# storage/permohonan, foto selfie di storage/profil) — jangan pernah masuk git.
/storage/

# Artefak build & deploy (dibangun ulang, jangan di-commit)
*.tar.gz
*.zip
*.stackdump

# Deploy secrets
deploy/.env
.ssh/saibatin-tunnel.ctl

# Jurnal kerja lokal (catatan sesi) — sengaja TIDAK masuk git & TIDAK ikut deploy.
# deploy/deploy.sh hanya mengirim .next/standalone + public + tessdata, jadi
# folder ini otomatis aman dari server; baris ini yang menahannya dari GitHub.
/sync-sam-journals/

# graphify: cost.json & cache lokal saja; graph.json + report sengaja
# di-commit supaya siapa pun yang clone langsung punya peta fungsinya.
/graphify-out/cost.json
/graphify-out/cache/
